PREFIX : <http://example.com/data/#> SELECT ?g ( avg ( ?p ) AS ?avg ) ( ( ( min ( ?p ) + max ( ?p ) ) / "2"^^<http://www.w3.org/2001/XMLSchema#integer> ) AS ?c ) WHERE {
 ?g <http://example.com/data/#p> ?p 
} GROUP BY ?g